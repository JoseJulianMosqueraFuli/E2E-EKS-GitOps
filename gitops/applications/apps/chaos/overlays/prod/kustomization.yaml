apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

commonLabels:
  environment: prod

# IMPORTANT: Chaos engineering in production requires mature SLOs, on-call coverage,
# and approval workflows. Do not enable automated chaos experiments in prod
# until Wave 4+ of the chaos engineering proposal is complete.
