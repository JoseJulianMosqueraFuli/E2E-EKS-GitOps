# PeerAuthentication resources enforce STRICT mTLS within MLOps namespaces.
# All pod-to-pod traffic must be encrypted with mutual TLS.
# Apply after Istio control plane and sidecar injection are configured.

---
# MLflow namespace: strict mTLS for all workloads
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: mlflow-strict-mtls
  namespace: mlflow
  labels:
    app.kubernetes.io/name: mlflow
    app.kubernetes.io/component: security
spec:
  mtls:
    mode: STRICT

---
# KServe namespace: strict mTLS for all workloads
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: kserve-strict-mtls
  namespace: kserve
  labels:
    app.kubernetes.io/name: kserve
    app.kubernetes.io/component: security
spec:
  mtls:
    mode: STRICT

---
# Monitoring namespace: strict mTLS for all workloads
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: monitoring-strict-mtls
  namespace: monitoring
  labels:
    app.kubernetes.io/name: monitoring
    app.kubernetes.io/component: security
spec:
  mtls:
    mode: STRICT

---
# Kubeflow namespace: strict mTLS for all workloads
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: kubeflow-strict-mtls
  namespace: kubeflow
  labels:
    app.kubernetes.io/name: kubeflow
    app.kubernetes.io/component: security
spec:
  mtls:
    mode: STRICT

---
# Argo Workflows namespace: strict mTLS for all workloads
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: argo-strict-mtls
  namespace: argo-workflows
  labels:
    app.kubernetes.io/name: argo-workflows
    app.kubernetes.io/component: security
spec:
  mtls:
    mode: STRICT

---
# Gatekeeper namespace: strict mTLS for all workloads
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: gatekeeper-strict-mtls
  namespace: gatekeeper-system
  labels:
    app.kubernetes.io/name: gatekeeper
    app.kubernetes.io/component: security
spec:
  mtls:
    mode: STRICT

---
# ArgoCD namespace: strict mTLS for all workloads
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: argocd-strict-mtls
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd
    app.kubernetes.io/component: security
spec:
  mtls:
    mode: STRICT

---
# Feast namespace: strict mTLS for all workloads
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: feast-strict-mtls
  namespace: feast
  labels:
    app.kubernetes.io/name: feast
    app.kubernetes.io/component: security
spec:
  mtls:
    mode: STRICT
