apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namePrefix: staging-

commonLabels:
  environment: staging

configMapGenerator:
  - name: kubeflow-config
    behavior: merge
    literals:
      - bucket_name=kubeflow-pipelines-staging
