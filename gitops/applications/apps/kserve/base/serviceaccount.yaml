apiVersion: v1
kind: ServiceAccount
metadata:
  name: kserve-sa
  namespace: kserve
  annotations:
    # Replace ACCOUNT_ID with your AWS account ID.
    # This is patched by environment overlays or CI/CD before deployment.
    eks.amazonaws.com/role-arn: arn:aws:iam::ACCOUNT_ID:role/kserve-irsa-role
